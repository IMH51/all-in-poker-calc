import { colors } from '.';

export const styles = {
    div: {
        color: colors.text,
        background: colors.background,
    },
    section: {
        color: colors.text,
        background: colors.background,
    },
    h1: {
        textAlign: 'center',
    },
    h2: {
        textAlign: 'center',
    },
    img: {
        width: '75px',
        zIndex: 1,
        cursor: 'pointer',
        borderWidth: '3px',
        borderStyle: 'solid',
        borderColor: 'transparent',
        borderRadius: '4px',
    },
};
