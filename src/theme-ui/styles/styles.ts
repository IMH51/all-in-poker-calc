import { colors } from '.';

export const styles = {
    div: {
        color: colors.text,
        background: colors.background,
    },
    section: {
        color: colors.text,
        background: colors.background,
    },
};
